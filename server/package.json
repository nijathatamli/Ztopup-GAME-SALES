{
  "name": "zelix-auth-backend",
  "version": "1.0.0",
  "description": "Production-ready Express + JWT + bcrypt authentication backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "check": "node --check index.js && node --check models/User.js && node --check routes/authRoutes.js && node --check controllers/authController.js && node --check middleware/auth.js && node --check utils/jwt.js && node --check utils/bcrypt.js && node --check config/index.js",
    "health": "curl -s http://localhost:3000/health"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.2.0",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
